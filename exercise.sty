\usepackage{ascmac}
\usepackage{chngcntr}

%\topmargin = 0mm
%\oddsidemargin = 5mm
%\textwidth = 152mm
%\textheight = 240mm
\topmargin = -20mm
\oddsidemargin = 0mm
\textheight = 245mm

% subsectionを 問1,問2 にする設定
\renewcommand{\thesubsection}{問\arabic{subsection}}

% subsubsectionを (1),(2)にする設定
\renewcommand{\thesubsubsection}{(\arabic{subsubsection})}
% (i),(ii)にする \arabic を \roman に変える．
% (a),(b)にするなら \alph に変える．

% 大問2の3番目の計算式のラベルを (2.3) にする設定
% 計算式の参照には \eqref{eq:hoge} を使う
\makeatletter
  \renewcommand{\theequation}{\arabic{subsection}.\arabic{equation}}
  \@addtoreset{equation}{subsection}
\makeatother

%% 丸角の囲みで左上に「問題1.1」の形式で番号を付ける環境．
%% 「問題x.y」のxはsection numberで，yはsection毎にリセットされる連番．
\makeatletter
    \newcounter{c@problem}
    \counterwithin*{c@problem}{section}
    \newenvironment{problem}[0]%
    {\stepcounter{c@problem}\begin{itembox}[l]{問題\arabic{section}.\arabic{c@problem}}}%
    {\end{itembox}}%
    \newenvironment{problem*}[0]%
    {\stepcounter{c@problem}\begin{itembox}[l]{問題}}% 
    {\end{itembox}}%
\makeatother

%% 「解答」と付けるだけの環境
\newtheorem*{answer}{解答}
